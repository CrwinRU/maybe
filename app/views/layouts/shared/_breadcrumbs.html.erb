<%# locals: (breadcrumbs:) %>

<div class="py-2 flex items-center gap-2">
  <% breadcrumbs.each_with_index do |(name, path), index| %>
    <% if index > 0 %>
      <%= icon("chevron-right", color: "gray", size: "sm") %>
    <% end %>

    <% translated_name = t("breadcrumbs.#{name.downcase.gsub(' ', '_')}", default: name) %>

    <% if path.present? && index < breadcrumbs.size - 1 %>
      <%= link_to translated_name, path, class: "text-sm text-gray-500 font-medium" %>
    <% elsif index == breadcrumbs.size - 1 %>
      <span class="text-primary font-medium text-sm"><%= translated_name %></span>
    <% else %>
      <span class="text-sm text-gray-500 font-medium"><%= translated_name %></span>
    <% end %>
  <% end %>
</div>
