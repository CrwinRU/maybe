---
en:
  rules:
    index:
      title: Rules
      new: New rule
      delete_all: Delete all rules
      empty_title: No rules yet
      empty_desc: Set up rules to perform actions to your transactions and other data on every account sync.
      sort_by: "Sort by:"
      ai_cost_warning: AI-enabled rule actions will cost money. Be sure to filter as narrowly as possible to avoid unnecessary costs.
      sort_name: Name
      sort_updated: Updated At
